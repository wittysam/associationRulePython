** trial1與trial2有部份Strong Association不同是正常的，因為在trial2我有將Confidence從0.66提高到0.7。
** 更嚴格的條件下，造成形成的Rule不同。因此並非程式碼有邏輯錯誤。

---------------------------------------------------------------------------------------------------------
** 在實作上使用原 Apriori Algoritm，往下生成Candidate Itemset的演算法（從k = 1 -> k = 2 -> k = 3..., etc.）
** 有透過k = 1時，從Database中刪除出現次數低於min_supp要求的Item，讓計算上可以加速。（原理為單一Item都無法超過min_supp
** ，則有其出現的k = n, n > 1的Itemset也無法滿足min_supp，因此刪除。）

---------------------------------------------------------------------------------------------------------
** python 的版本使用為 3.8.8

---------------------------------------------------------------------------------------------------------
<Directory Structure>
|
---- E24096027.zip
    |
    |---- associationRule.py (主程式碼)
    |
    |---- readme.txt (本檔案)
    |
    |---- input.txt (主程式碼輸入)
    |
    |---- trial1.txt (min_supp = 0.1, min_conf = 0.66)
    |
    |---- trial2.txt (min_supp = 0.05, min_conf = 0.7)
    |
    |---- trial3.txt (min_supp = 0.01, min_conf = 0.66)